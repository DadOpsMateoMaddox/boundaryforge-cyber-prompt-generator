# Core dependencies
# BoundaryForge uses only the Python standard library for its engine.
# Optional dependencies below support dashboards, YAML exports, and promptfoo.

# Dashboard
streamlit>=1.30.0

# YAML export / promptfoo config writing
pyyaml>=6.0

# Testing
pytest>=7.0.0
